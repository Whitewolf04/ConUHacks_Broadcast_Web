# Broadcast Webapp (ConUHacks VII)

## To install dependecies

Run `npm install` in directory `src/front` and `src/back`

## To start

Start the backend: `cd src/back && npm start`

Start the frontend `cd src/front && npm start`